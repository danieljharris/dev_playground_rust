
# Run with timings
```
clear && cargo build --release && time target/release/dev_playground_rust
```

```
clear && rustup run nightly cargo build --release && time target/release/dev_playground_rust
```